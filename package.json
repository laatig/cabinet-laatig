{
  "name": "cabinet-laatig",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd server && npx prisma generate && npm run build && cd ../client && npm install && npm run build",
    "db:migrate": "cd server && npx prisma migrate dev --name init",
    "db:seed": "cd server && npx prisma db seed",
    "db:reset": "cd server && npx prisma migrate reset --force",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "playwright": "^1.61.0"
  }
}
